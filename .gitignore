# Reference clones — kept on disk for grep/handover, not part of this repo's history
/reference/

# Node / build
node_modules/
dist/
.cache/
.output/
.wxt/
*.log
*.tsbuildinfo

# Env / secrets
.env
.env.*
!.env.example
# Extension ships as a static build (not deployed via Coolify), so its production
# service endpoints must be committed and baked in at build time — public URLs only.
!apps/extension/.env.production
!apps/mobile/.env.production

# OS
.DS_Store

# Confidential circuits build output (reproducible via nargo compile)
circuits/target/
circuits/**/target/

# Soroban contracts build output
contracts/target/
.playwright-mcp/

# Docs app generated
.source/
.react-router/
build/

# scratch
/tmp-*

# Video production (another agent's in-progress work — kept local, out of the repo)
videos/

# AI-agent working files (kept local, out of the public repo)
.thoughts/
.agents/
.claude/
.cloud/
reference/
AGENTS.md
CLAUDE.md
skills.log
skills.log*
